# Environment Variables for MTG Deck Simulator

# Application Credentials (REQUIRED for production)
APP_USERNAME=admin
APP_PASSWORD=change-me-in-production

# Session Configuration (REQUIRED for production)
SESSION_SECRET=generate-a-secure-random-string-for-production

# Environment
NODE_ENV=development

# Server Configuration
PORT=3000

# Security Notes:
# 1. Generate a secure random session secret (minimum 32 characters)
# 2. Use strong credentials for APP_USERNAME and APP_PASSWORD
# 3. Set NODE_ENV=production in production environments
# 4. Consider using a proper authentication system instead of hardcoded credentials